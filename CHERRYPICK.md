# Cherry-Pick

`c94e03a` on `lab09-feature` and `aa740de` on `lab09-hotfix` are the same change, but they're two separate commits with different hashes. Cherry-picking copies a commit onto a new branch, it doesn't move the original.